## Playgrond repo for testing features of _pytorch-lightning_ framework

### Structure

- `main.py`, `model.py` and `load_dataset.py` contain generic pipeline with feature explaration of pytorch-lightning

**License**: CC-BY-SA
